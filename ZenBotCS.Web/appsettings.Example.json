{
  "//": "Copy this file to appsettings.json (git-ignored) and fill in real values.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//BotDbConnectionString": "MySQL connection string for the bot database (same DB the bot uses).",
  "BotDbConnectionString": "server=localhost;port=3306;database=zenbot;user=zenbot;password=...",

  "//CocApiCacheConnectionString": "The CoC cache DB (same as the bot's). Read-only here, used to resolve clan names.",
  "CocApiCacheConnectionString": "server=localhost;port=3306;database=zenbotcache;user=zenbot;password=...",

  "//MySqlServerVersion": "Your DB server version, e.g. '8.0.0-mysql' or '10.11.0-mariadb'. Avoids a connection at startup.",
  "MySqlServerVersion": "8.0.0-mysql",

  "Discord": {
    "//ClientId": "Discord application Client ID (Developer Portal > OAuth2).",
    "ClientId": "...",
    "//ClientSecret": "Discord application Client Secret.",
    "ClientSecret": "...",
    "//BotToken": "Bot token, used to look up a member's roles in the guild for authorization.",
    "BotToken": "...",
    "//GuildId": "The community server (guild) id used for membership/role checks.",
    "GuildId": "000000000000000000",
    "//RequiredRoleId": "Discord role id that grants roster access (e.g. the bot's FamilyLeadershipRoleId). Users without it can sign in but see nothing.",
    "RequiredRoleId": "000000000000000000"
  }
}
